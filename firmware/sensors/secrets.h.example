// Copy to secrets.h and fill in. secrets.h is gitignored — never commit it.
#pragma once

#define WIFI_SSID      ""
#define WIFI_PASSWORD  ""

// The laptop running the backend, on the same LAN. Find it with `ipconfig`.
// This is DHCP and will change when the laptop reconnects to WiFi.
#define BACKEND_URL    "http://192.168.1.100:8000"
